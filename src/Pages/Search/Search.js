import React, { useState } from "react";

const Search = () => {
	const [type, setType] = useState(0);

	return (
		<div>
			<span className="pageTittle">Search</span>
		</div>
	);
};

export default Search;
